# Standard configuration for ESP-PROG and ESP32 target

# Load the interface configuration for the ESP-PROG debugger
source [find interface/esp_usb_jtag.cfg]

# Load the target configuration for the ESP32 chip
source [find target/esp32.cfg]